import { ResourceOptions } from '../../help/type/IResource';

const CustomDomainResource: ResourceOptions = {
    name: '自定义域名管理',
    value: 'customDomain',
    order: 200,
};

export default CustomDomainResource;
