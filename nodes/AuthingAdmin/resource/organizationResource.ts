import { ResourceOptions } from '../../help/type/IResource';

const OrganizationResource: ResourceOptions = {
    name: '组织与部门管理',
    value: 'organization',
    order: 600,
};

export default OrganizationResource;
